# JSONs de datos de negocio — datos internos, no van a repo público
bodegas_gestion.json
bodegas_ir_otras.json
merma_isabel_riquelme.json
CATALOGO PRODUCTOS/Datos.json
catalogo-bot.json
catalogo-cotizador.json

# Credenciales — NUNCA subir
credenciales_db.ini
credenciales_db.enc
credenciales_erp.ini
_PROMPTS/credenciales_dbRP.ini
CLAVE*.txt
CLAVE*.ini
serviceAccountKey*.json
*.env
.env.*
users_export.json

# Scripts pipeline — no van a GitHub
BODEGAS/
!BODEGAS/descargar_blazor_bodegas.py
VENTAS EL MANZANO/
DATOS ERP/
data/
backups/
logs/
_ARCHIVADOS/
_HISTORICO/
_ARCHIVO_FERRETERIA/
_PROMPTS/
_utilidades/
ADQUISICIONES/

# Archivos binarios / Excel
*.xlsm
*.xlsx
*.xls
*.mp4

# Python
__pycache__/
*.pyc
*.pyo

# Claude
.claude/

# open-code-review: config global tiene auth_token — NUNCA subir
.opencodereview/config.json

# Centinela pipeline (generado por procesar-actualizacion.py, solo local)
.pipeline_date

# Token temporal generado por rotate_token.py (GitHub Actions)
.new_token_value

# Temporales Office
~$*.docx
~$*.xlsx
