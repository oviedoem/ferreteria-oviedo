rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Para desarrollo - permitir acceso completo
    // EN PRODUCCIÓN: Cambia a reglas con autenticación
    match /{document=**} {
      allow read, write: if true;
    }
  }
}