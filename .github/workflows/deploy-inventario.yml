name: Deploy APP-INVENTARIO a GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'APP-INVENTARIO/**'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurar GitHub Pages
        uses: actions/configure-pages@v5

      - name: Subir APP-INVENTARIO como artefacto
        uses: actions/upload-pages-artifact@v3
        with:
          path: APP-INVENTARIO

      - name: Deploy a GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
